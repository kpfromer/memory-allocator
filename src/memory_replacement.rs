pub struct MemoryReplacement {
    frame_size: usize,
    frames: Vec<Vec<i32>>,
    // deque: VecDeque<i32>,

    // fn new(frames: usize, accesses: usize) -> Self;
    // fn run(&mut self, accesses: &Vec<i32>) -> MemoryAccesses;
    // fn gen_table(&self) -> Table;
}

impl MemoryReplacement {
    fn add() {}
    fn replace() {}
    fn upsert() {}

    fn run() -> MemoryAccesses {}
}
